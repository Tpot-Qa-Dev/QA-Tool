# Dependencies
node_modules/

# Environment — never commit secrets
.env
backend/.env

# SSH keys — never commit
id_ed25519
id_ed25519.pub
*.pem

# Build output
frontend/dist/

# Persisted audit reports
backend/reports/

# Runtime tool settings
backend/settings.json

# Cumulative token-usage counter
backend/usage.json

# Operator-defined custom checks
backend/custom-checks.json

# Editable prompt versions (runtime state)
backend/prompt-config.json

# AI model profiles (contains per-model API keys — never commit)
backend/ai-models.json

# Project-wise Figma access tokens (contains secrets — never commit)
backend/figma-projects.json
backend/*.tokens.json

# Logs
*.log
npm-debug.log*

# OS / editor / tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
 